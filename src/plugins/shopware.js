import { createInstance } from '@shopware-pwa/api-client'

const shopwareConfig = {
    endpoint: 'https://shopware-6-demo.shop-studio.io',
    accessToken: 'SWSCV3UXRXPAR3BOQ0FNZUJYVA',
}

createInstance(shopwareConfig)
